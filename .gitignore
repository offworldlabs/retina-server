node_modules/
dist/
build/
__pycache__/
*.pyc
.env

# Written on the deploy target while a production deploy is mid-flight, and
# read by the rollback job. Host state, never committed.
.deploy-in-progress
.venv/
venv/
*.egg-info/
.DS_Store
digital_ocean
digital_ocean.pub
tar1090_data/
coverage_data/
metro_tower_cache.json
tower_assignments.json
other/
backend/.coverage
backend/.coverage.*
backend/htmlcov/

# Local-only agent customizations (server credentials, ops, private prompts — never push)
.github/instructions/
.github/prompts/
backend/data/events.json
backend/data/state_snapshot.json
backend/data/state_snapshot.json.sha256
# Fleet ground-truth dump. The orchestrator's --ground-truth-path defaults to a
# relative 'ground_truth.json', and `just up` runs it with cwd=backend, so a
# native dev run drops one here. (The compose fleet writes /app/data/ instead.)
backend/ground_truth.json
backend/data/*.json.migrated
backend/data/users.db
backend/data/users.db-shm
backend/data/users.db-wal
.vscode/*
.claude/*
skills-lock.json
frontend/test-results/.last-run.json
.playwright-mcp/*

# Ad-hoc PNG screenshots captured during dev/QA in the repo root
/test-radar-*.png
/testradar-*.png
/testmap-*.png
/local-livemap-*.png
/postdeploy-*.png

# Auto-generated backend runtime state + python coverage artefacts.
# .coverage.* covers pytest-cov's parallel-mode fragments
# (.coverage.<host>.<pid>.<rand>) — one of those slipped into 3b4267f
# because only the bare filename was ignored, and every subsequent test
# run's coverage-combine deleted it again, leaving a permanent phantom
# deletion in git status.
backend/data/runtime/
.coverage
.coverage.*

# local testmap runner (justfile) pids/logs
.testmap-run/

# uv regenerates this on every `uv run`; the pins live in requirements.txt
backend/uv.lock

# Planning scratch: kept locally, never published
docs/superpowers/
