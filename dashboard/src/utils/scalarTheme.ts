/**
 * Scalar's colours in RETINA's palette, for the reference viewer at /api-docs.
 * The public reference serves the same file; its header says why the values
 * are written out. By path, since a `?raw` query does not survive the shared
 * package's exports map.
 */
import css from "../../../packages/shared/css/scalar-theme.css?raw";

export const SCALAR_THEME_CSS = css;
