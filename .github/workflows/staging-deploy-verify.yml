name: Staging deploy and verify

# Deploy staging, then prove it, as one unit. ci.yml calls this from a single
# job that carries the `staging-deploy` concurrency group, and a calling job is
# not complete until every job below has finished, so the group is held from
# the first byte of the deploy to the last E2E assertion.
#
# That holding is the whole point of the file, and it is why the jobs below
# cannot simply live in ci.yml with a group each. Concurrency is acquired and
# released per job, so a following run's deploy was free to land between one
# run's smoke tests and its E2E — which is how three merges on 2026-09-10 all
# failed, none of them for a fault in the change: the box was restarting
# underneath a suite that was testing it.
#
# Splitting these back into separate jobs in the caller reopens that race,
# whatever concurrency group each of them carries.

on:
  workflow_call:
    inputs:
      app_dir:
        description: >-
          Deploy directory on the droplet. Threaded in from ci.yml's
          `env.APP_DIR` rather than restated here: workflow-level `env` does not
          cross a `workflow_call` boundary, and a second definition is a second
          thing to forget when the directory moves.
        required: true
        type: string
      playwright_image:
        description: >-
          Playwright container image for the E2E job, derived from the lockfile
          by ci.yml's `playwright-image` job so the browsers cannot disagree
          with the client that drives them.
        required: true
        type: string
    secrets:
      STAGING_HOST:
        required: true
      STAGING_SSH_KEY:
        required: true
      STAGING_RADAR_API_KEY:
        required: true
      # Inert while unset: the smoke script and Playwright both render them
      # empty and send no Access headers, which is the state of an environment
      # with no Service Auth policy in front of its admin surface.
      CF_ACCESS_CLIENT_ID:
        required: false
      CF_ACCESS_CLIENT_SECRET:
        required: false

jobs:
  deploy:
    name: Deploy to staging
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Carries the deploy directory as an environment variable rather than only
    # interpolating it: `envs: APP_DIR` below forwards it to the droplet, where
    # deploy/pre-deploy.sh and deploy/rollback.sh read it.
    env:
      APP_DIR: ${{ inputs.app_dir }}
    steps:
      - name: Deploy to staging via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: root
          key: ${{ secrets.STAGING_SSH_KEY }}
          command_timeout: 20m
          envs: APP_DIR
          script: |
            set -e
            cd ${{ env.APP_DIR }}

            # Same two wrong-environment checks production runs, first and in the
            # same order, for the same reasons. The hostname is the only signal
            # that holds on a box with no .env at all, which is exactly the state
            # a droplet awaiting its first deploy is in; see the production block
            # for why it is an exact match rather than a pattern.
            if [ "$(hostname)" != "retina-staging" ]; then
              echo "::error::Refusing to deploy staging onto '$(hostname)' — expected retina-staging. If this droplet was rebuilt, finish provisioning it (hostnamectl set-hostname retina-staging); otherwise this job is pointed at the wrong box."
              exit 1
            fi

            # Same check as ci.yml's production deploy, which has the reasoning:
            # a marker is a box left mid-deploy, and snapshotting over it would
            # lose the rollback point.
            if [ -f .deploy-in-progress ]; then
              echo "::error::${{ env.APP_DIR }}/.deploy-in-progress is still present on $(hostname) ($(cat .deploy-in-progress)). An earlier deploy was left mid-flight and never confirmed healthy. Inspect the box and recover it (bash deploy/rollback.sh, or fix forward), then delete the marker and re-run. Refusing to deploy; the running stack is untouched."
              exit 1
            fi

            # A .env naming another environment means this job is pointed
            # at a box it does not own. Without this the write further down simply
            # takes that box over: overwriting its .env and rebuilding would bring
            # production back on staging's hostnames and container names. A
            # MISSING .env is allowed through; that is the normal state
            # of a box awaiting its first deploy, and we write it below.
            if [ -f .env ] && ! grep -q '^COMPOSE_FILE=.*docker-compose\.staging\.yml' .env; then
              echo "::error::${{ env.APP_DIR }}/.env exists but does not select the staging overlay — this host is not staging. Inspect it; do not deploy."
              exit 1
            fi

            # Pre-flight: Cloudflare's origin-pull CA, checked BEFORE anything
            # rebuilds the stack. nginx renders `ssl_verify_client on`
            # (deploy/nginx/snippets/tls.conf) and start.sh refuses to boot
            # without the CA, so on a droplet where the human half of this work
            # is not done (enable Authenticated Origin Pulls for the zone, put
            # the CA on the box) the rebuild below would swap in a container
            # that refuses to start and leave the environment DOWN. Refusing
            # here converts that outage into a refused deploy: everything above
            # this line only reads the box.
            CF_CA=/etc/ssl/cloudflare/origin-pull-ca.pem
            if [ ! -f "$CF_CA" ]; then
              echo "::error::${CF_CA} is missing on $(hostname). nginx requires Cloudflare's origin-pull CA and will not start without it. Enable Authenticated Origin Pulls for the zone, then fetch the CA onto this droplet (curl -fsS --create-dirs -o ${CF_CA} https://developers.cloudflare.com/ssl/static/authenticated_origin_pull_ca.pem) and re-run. Refusing to deploy; the running stack is untouched."
              exit 1
            fi
            if ! openssl x509 -in "$CF_CA" -noout >/dev/null 2>&1; then
              echo "::error::${CF_CA} exists on $(hostname) but is not a parseable PEM certificate — a truncated download or an error page saved to the path would look exactly like this. nginx would fail to start. Replace it and re-run. Refusing to deploy; the running stack is untouched."
              exit 1
            fi
            echo "Origin-pull CA present and parseable: ${CF_CA}"

            # Pre-flight: refuse to deploy when the disk is near full. Without
            # this, git fetch silently writes a partial pack, the source tree
            # never advances, and `docker compose --build` happily caches every
            # layer and "succeeds" with yesterday's image (run 25923117075).
            AVAIL_MB=$(df -m / | awk 'NR==2 {print $4}')
            if [ "${AVAIL_MB:-0}" -lt 2048 ]; then
              echo "::error::Staging disk only ${AVAIL_MB}MB free (< 2GB threshold). Aborting deploy."
              df -h /
              docker system df 2>/dev/null || true
              exit 1
            fi

            # Pre-flights go above this line. From here the box is mid-deploy
            # and a failure below is the rollback job's to undo, keyed on the
            # marker's run id. NOT `|| true`: no rollback point, no deploy.
            bash deploy/pre-deploy.sh
            echo "run ${{ github.run_id }} started $(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              > ${{ env.APP_DIR }}/.deploy-in-progress

            # Hard-reset rather than `git pull` — see the production deploy
            # block for the full incident description; same failure mode would
            # surface here if the staging server ever picked up a local commit.
            # `set -e` above guarantees a failed fetch/reset aborts the deploy.
            git fetch origin main
            git reset --hard origin/main
            git submodule update --init --recursive
            # The host's ./.env sets COMPOSE_FILE (base + staging overlay), so the
            # compose commands below match the production deploy block exactly:
            # staging runs no fleet either. Written from the tree on every deploy
            # so a rebuilt droplet or a hand-edited .env cannot select the wrong
            # overlay.
            cp deploy/env.staging.example .env
            # Top up with this host's own CARTO basemap key — see the note in
            # deploy/env.<env>.example. Guarded rather than chained with &&: a host
            # with no key must build watermarked tiles, not fail its deploy.
            if [ -f /root/.secrets/carto.env ]; then cat /root/.secrets/carto.env >> .env; fi
            # Each build orphans the image it replaces and nothing else on the
            # box removes them, so they accumulate until the pre-flight above
            # refuses the deploy. Dangling only: `-a` goes by what no container
            # references, which is exactly the rollback point tagged above.
            # Before the builder prune, which cannot free a layer an image holds.
            docker image prune -f
            # Cap build cache at ~10GB (LRU eviction) rather than wiping it all:
            # keeps recent layers so incremental builds stay fast, and bounds
            # total size so cache growth can't trip the disk pre-flight above.
            # The stale-layer failure the old blanket wipe guarded against was
            # rooted in disk-full (partial git pack -> source never advanced ->
            # cache reused); the disk pre-flight now catches that root cause, so a
            # full wipe is redundant and only forced slow from-scratch rebuilds.
            docker builder prune -f --keep-storage 10GB
            # Only `server`: staging runs no fleet simulator (docker-compose.
            # staging.yml keeps it behind the same unenabled `sim` profile as
            # production), so `server` is the whole of the staging deploy, as it
            # is of production's. Naming `fleet` here would auto-enable its
            # profile and bring it back.
            # No `docker compose down` before this: `up --build` swaps the container
            # only once the image exists, so a failed build leaves the old one
            # serving, and an unchanged image is not swapped at all.
            # --remove-orphans because this deploy renamed the compose
            # service. container_name is pinned, so the container kept by the
            # old service key is an orphan holding the exact name the new one
            # needs, and compose leaves orphans alone by default: the create
            # then fails with a name conflict and the stack does not come up.
            # Verified on retina-test before this landed. It stays afterwards
            # as the general guard for a removed or renamed service.
            docker compose up -d --build --remove-orphans server
            # Wait for server to become healthy (up to 90s)
            for i in $(seq 1 18); do
              if docker compose exec -T server \
                python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')" 2>/dev/null; then
                echo "Staging server healthy after ~$((i*5))s"
                break
              fi
              echo "Waiting for health check... attempt $i/18"
              sleep 5
              if [ "$i" = "18" ]; then
                echo "Staging health check failed after 90s"
                docker compose logs --tail=50
                exit 1
              fi
            done

            # Reached only by the loop's `break`: the app answered /api/health,
            # so the box is no longer mid-deploy.
            rm -f ${{ env.APP_DIR }}/.deploy-in-progress

            # The fleet container from before the simulator left staging. A
            # profile-gated service is invisible to `up --remove-orphans`, so
            # the container it left behind neither restarts nor gets removed by
            # the deploy above; it would keep POSTing frames at a server that
            # no longer mounts the ingest path. Idempotent once it is gone.
            docker rm -f retina-staging-fleet >/dev/null 2>&1 || true
  rollback:
    name: Roll back staging if the deploy failed after it began
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [deploy]
    # Keyed the same way as ci.yml's rollback-production-on-deploy-failure;
    # see there for why. No concurrency group of its own: the calling job
    # holds `staging-deploy` until every job here has finished (see the
    # header), so a following run's deploy cannot enter the directory while
    # this runs. The marker checks below make any run whose deploy never
    # started, cancelled while queued included, a no-op or a refusal rather
    # than an action.
    if: >-
      always() && (
        needs.deploy.result == 'failure' ||
        needs.deploy.result == 'cancelled'
      )
    env:
      APP_DIR: ${{ inputs.app_dir }}
    steps:
      - name: Roll back, but only if the deploy actually mutated the box
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: root
          key: ${{ secrets.STAGING_SSH_KEY }}
          command_timeout: 15m
          envs: APP_DIR
          script: |
            set -e
            # rollback.sh resets the tree and retags images over :latest, so a
            # STAGING_HOST pointed at the wrong droplet does more damage here
            # than a deploy would.
            [ "$(hostname)" = "retina-staging" ] || { echo "::error::Refusing to roll back on '$(hostname)': expected retina-staging."; exit 1; }
            cd ${{ env.APP_DIR }}

            # No marker: the deploy refused in a pre-flight with the stack
            # untouched, or it had already cleared the marker with the app
            # healthy on the new build. Neither is a rollback case; ci.yml's
            # production pair has the reasoning.
            if [ ! -f .deploy-in-progress ]; then
              echo "No .deploy-in-progress marker: the deploy either refused before touching the box or failed after the app was already healthy on the new build. Nothing to roll back."
              exit 0
            fi

            MARKER=$(cat .deploy-in-progress)

            # Another run's marker: this run touched nothing and cannot tell
            # a deploy still in progress or an unrecovered outage from a box
            # already fixed by hand, so stop rather than guess (see ci.yml).
            case "$MARKER" in
              "run ${{ github.run_id }} "*) ;;
              *)
                echo "::error::${{ env.APP_DIR }}/.deploy-in-progress was left behind by a different run (${MARKER}). Either that run is still deploying this box, in which case leave the marker to it, or an earlier deploy was left mid-flight and never confirmed healthy. Check the Staging runs before touching anything; this run touched nothing."
                exit 1
                ;;
            esac

            echo "Deploy failed after it began mutating the box (${MARKER}). Rolling back..."

            # rollback.sh distinguishes three outcomes and `set -e` would
            # collapse the last two, so take its status explicitly.
            set +e
            bash deploy/rollback.sh
            rc=$?
            set -e

            case "$rc" in
              0)
                rm -f .deploy-in-progress
                echo "Rollback complete: staging is back on the previous build."
                ;;
              2)
                # Service restored with the database left ahead of the code.
                # The box is no longer mid-deploy, so the marker goes; the run
                # stays red so the report above gets read.
                rm -f .deploy-in-progress
                echo "::error::Staging is back up, but a database downgrade is outstanding: see the report above before trusting this rollback."
                exit 2
                ;;
              *)
                # Marker left in place: the box is still mid-deploy, which is
                # exactly what the next run must know.
                echo "::error::Rollback FAILED (exit ${rc}). Staging may still be down: SSH to retina-staging and recover by hand, then delete ${{ env.APP_DIR }}/.deploy-in-progress so the next deploy is not blocked by it."
                exit "$rc"
                ;;
            esac
  smoke-tests:
    name: Staging smoke tests
    runs-on: ubuntu-latest
    timeout-minutes: 35
    needs: [deploy]
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke tests against staging
        # No fleet gate ahead of this any more: staging runs no simulator, so
        # there is nothing to wait for between the deploy and the suite, and the
        # suite asserts the opposite (deploy/staging-smoke-test.sh, "No
        # simulator here").
        timeout-minutes: 15
        # Same service token as production: one token, admitted by a Service Auth
        # policy on each admin application. Inert while the secrets are unset.
        env:
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: bash deploy/staging-smoke-test.sh
  e2e:
    name: Playwright E2E (staging)
    runs-on: ubuntu-latest
    # Only the pull and npm ci sit outside the test step's own cap now, and an
    # image pull is minutes at worst. See the production job for why the job
    # cap is a backstop rather than the primary bound.
    timeout-minutes: 30
    needs: [smoke-tests]
    # The browsers and their shared libraries ship in the image, so there is no
    # install step and neither apt nor Playwright's CDN is in this path. Both
    # broke repeatedly on 2026-08-18 and 2026-08-19; see the production job.
    container:
      image: ${{ inputs.playwright_image }}
    # `npm ci` runs @playwright/test's postinstall, which fetches browsers. The
    # image sets PLAYWRIGHT_BROWSERS_PATH=/ms-playwright and the tag is derived
    # to match the lockfile, so it finds the right revision already there and
    # does nothing. This makes that a guarantee rather than an expectation: if
    # the browsers were ever NOT found, the postinstall would quietly download
    # them from the CDN this job exists to avoid, and the only symptom would be
    # a slow green run. With this set it fails loudly at the browser instead,
    # pointing at the cause. See microsoft/playwright#4283.
    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
    steps:
      - uses: actions/checkout@v4
      # Kept despite the image shipping its own Node: this pins the version the
      # tests run under to the same 20 as every other job, rather than letting
      # it drift with whatever the Playwright image happens to carry, and keeps
      # the npm cache. Only browser provisioning changes here.
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - name: Run E2E tests (staging)
        # Has to stay above what nodes.spec.ts's two retried beforeAll hooks can
        # reach, so a node that never propagates fails an assertion rather than
        # timing the step out and losing the report. That spec derives its hook
        # budgets from the server's refresh cadence; raise this if it widens.
        timeout-minutes: 20
        run: npm run test:e2e:staging -w e2e
        env:
          CI: "true"
          RADAR_API_KEY: ${{ secrets.STAGING_RADAR_API_KEY }}
          # Passage through Access for the admin surface. Playwright follows the
          # edge's 302, so without these the admin tests would parse Cloudflare's
          # login page as the app. Empty secrets render empty and send nothing.
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
